# Projektstatus

## Stand

- Status: Release `v2026.05.08-1` veroeffentlicht und upgrade-/restore-rehearsal-validiert. Welle 15a-15f komplett (User-Feedback-Cluster aus erster UI-Probelauf-Runde): Bug-Fixes, KPIs klickbar + DE-Hilfe, Fundamentals-Vollausbau, Datumsfilter, native Currency pro Asset, User-Display-Currency mit FX-Konvertierung. Welle 16a ML-Ensemble (XGBoost + LightGBM + RandomForest). AGENTS.md als projekteigene Agent-Constitution im Repo. 231 Unit-Tests OK, API+UI-Regression durchgehend gruen.
- Letzte Aktualisierung: 2026-05-12
- Aktive Arbeit: API- und UI-Regression sind als Gates verankert; der verlustfreie Docker-Hub-Deploy-/Upgrade-Pfad ist fuer Release `2026.05.07-1` durchgeprobt, HTTP-Logs tragen Request-ID und redaktierte Audit-Felder, Phase 1 liefert normalisierte Assetmetadaten, Watchlist-Tags, aggregierte Watchlist-News, priorisierte Watchlist-Alerts, Provider-Coverage im Dashboard, einen optionalen Alpha-Vantage-Livepfad fuer ETFs und Krypto, einen Symbol-Research-Schnitt auf `/api/research/{symbol}` mit sichtbarem `Provider Research`-Panel auf `/analysis/<symbol>`, persistiertes Watchlist-Alert-Management mit Popup-/Push-Eligibility sowie einen serverseitigen periodischen Watchlist-Push-Dispatcher mit Delivery-Deduplizierung; der automatische GitHub-Actions-Publish-Pfad ist mit echten Docker-Hub-Secrets live bestaetigt, der lokale Betriebsweg laedt fuer wiederkehrende Sitzungen eine persistente `.env.local` als private Override-Datei fuer Keys und persoenliche Zugangsdaten; produktive Push-/VAPID-Haertung ist umgesetzt, gepusht, mit lokalen Ziel-VAPID-Werten validiert und als Release `2026.05.07-1` upgrade-/restore-verifiziert

## Gesichert verifiziert

- Welle 15f User-Display-Currency + FX-Konvertierung am 2026-05-12: kompletter Schnitt vom FX-Provider bis zum UI-Hook. (a) Neuer `app/fx_service.py` mit Singleton + `get_rates(base="USD")` gegen `https://api.frankfurter.app/latest?from=USD&to=EUR,GBP,CHF,JPY,CAD,AUD,CNY` (ECB-Reference-Rates, kein API-Key). 60min-Cache pro Base-Currency, defensiv (None bei HTTP-Error/Invalid-JSON). Modul-Konstante `SUPPORTED_CURRENCIES = (USD, EUR, GBP, CHF, JPY, CAD, AUD, CNY)`. Rate-Limit-Bucket `fx` (1.0 req/s, capacity 5). (b) Endpoint `GET /api/fx/rates?base=USD` (no-auth, public). Bei Provider-Fail: degraded-Payload mit Identity-Rate damit das UI weiter rendert. (c) Alembic-Migration `0008_add_user_display_currency` (revision `d5e6f7a8b9c0`) fuegt `users.display_currency String(8) NOT NULL DEFAULT 'USD'` hinzu. SQLAlchemy-Model `User.display_currency`. `PortfolioSettingsResponse`/`Request` um `display_currency` erweitert. `PUT /api/auth/me/portfolio-settings` validiert gegen `SUPPORTED_CURRENCIES`, faellt silent auf USD zurueck wenn unsupported (verhindert 400 bei aelteren Clients ohne Feld). Audit-Eintrag enthaelt das neue Feld. Backup/Export/Import-Coverage. (d) Frontend `useDisplayCurrency`-Hook: liest `/api/auth/me/portfolio-settings`, spiegelt auf `localStorage`-Key `displayCurrency` damit der erste Paint nach Reload nicht flackert; faellt auf USD zurueck wenn unauthenticated. `useFxRates(base)`-Hook: React-Query gegen `/api/fx/rates`, staleTime 30min, refetch 60min. `convertMoney(amount, from, to, rates)`-Helper: defensiv (gleiche Currency = no-op, fehlende Rate = no-op). SettingsPage-Portfolio-Section bekommt einen Currency-Select-Dropdown mit den 8 supported Codes. AnalysisPage Quote-Header zeigt konvertierten Wert in `displayCurrency` und einen Native-Hint darunter (`{nativeValue} {nativeCurrency} → {displayCurrency}`). 231 Unit-Tests OK (vorher 224 + 7 `FxServiceTests`: Normalisierung mit Identity-Entry, Cache-vermeidet-zweiten-HTTP-Call, unsupported-Base-noopt, HTTP-Error-returnt-None, Invalid-Payload-returnt-None, Rates-strippen-Non-Numeric, supported-set). API+UI-Regression weiter gruen (das neue Frontend-Bundle baut ohne TypeScript-Fehler trotz neuer Hooks + Imports).
- Welle 15e Currency-Anzeige pro Asset am 2026-05-12: drei minimalinvasive Schnitte fuer die Symbol-Currency-Sichtbarkeit. (a) `services.get_stock_data` baut den `info`-Block mit `currency` aus `tickerInfo.get("currency")` und `"USD"` als Fallback — yfinance liefert das Feld bereits, der Backend-Pfad reicht es jetzt zuverlaessig durch. (b) `fmp_service.normalized_ticker_info` ergaenzt um `currency` aus `profile.get("currency")` damit der FMP-Fallback dieselbe Information liefert (None-Stripping bleibt aktiv, kein Bruch fuer Provider ohne Currency). (c) `twelve_data_service.normalized_ticker_info` analog ergaenzt um `currency` aus `profile.get("currency") or quote.get("currency")`. Frontend `StockResponse.info`-Type um `currency`-Feld erweitert. Quote-Header auf `/analysis/<symbol>` zeigt jetzt den lastClose mit Currency-Suffix (Default USD). `FundamentalsSection` haengt Currency an Market-Cap, 52W-High und 52W-Low. `FundamentalsDetailSection` (aus Welle 15c) nutzte `detail.currency` schon — dadurch sind alle expliziten Money-Werte auf der AnalysisPage jetzt Currency-bewusst. PaperTrading-/Dashboard-Money-Anzeigen bewusst nicht angefasst: Welle 15f bringt die User-Display-Currency-Konvertierung. 224 Unit-Tests OK (keine neuen Tests noetig, currency-Feld ist Pass-Through; existing `test_normalized_ticker_info_covers_yfinance_subset` bleibt kompatibel weil neue Felder nur zusaetzlich sind). API+UI-Regression weiter gruen.
- Welle 15d Datumsfilter am 2026-05-12: zwei kleine Schnitte. (a) **Chart-Range-Bugfix auf AnalysisPage**: `TIMEFRAMES`-Konstante hatte `value: "MAX"`, aber Backend-`PERIOD_MAP` kennt nur `"ALL"` — der MAX-Button fiel daher stillschweigend auf den 6M-Default zurueck. Wert auf `"ALL"` korrigiert, Label "Max" bleibt. Damit zeigt MAX jetzt tatsaechlich die Full-History (Backend `("max", "1wk")`). (b) **Trade-Journal-Range-Filter auf PaperTradingPage**: neuer `journalRange`-State (`"7d" | "30d" | "90d" | "1y" | "all"`) mit Default `"all"` (kein Regressions-Bruch). `JOURNAL_RANGE_DAYS`-Map verarbeitet den Range zu einem `Date.now() - days*86400000`-Cutoff. `filteredTransactions`-Memo filtert clientseitig nach `executedAt`-Timestamp; `journalTotals` rechnet die Realized/Fees/Tax-Totals aus dem gefilterten Datensatz. Button-Strip mit ARIA-`group` und `aria-pressed`-States ueber den fuenf Ranges, plus Counter-Hint "X of Y shown" wenn Range != all. Empty-State `journal.emptyInRange` wenn der Filter alle Transactions herausfiltert. i18n DE+EN unter `paperTrading.journal.range.*` (7 Strings: title + 5 range labels + filteredCount-Placeholder). Help-Topics `paper-trading.md` + `paper-trading.de.md` ergaenzt. 224 Unit-Tests OK (rein clientseitiger Schnitt). API+UI-Regression weiter gruen.
- Welle 15c Fundamentals-Vollausbau am 2026-05-12: Neuer Backend-Pfad `fmp_service.normalized_fundamentals_detail(symbol)` aggregiert FMP `/profile`, `/key-metrics-ttm`, `/ratios`, `/income-statement?period=annual&limit=1` und `/historical-price-full/stock_dividend`. Output-Schema: `isin`, `wkn` (mechanisch aus DE-ISIN abgeleitet via `isin_to_wkn`-Helper, Positionen 6-11 des ISIN-Strings), `cusip`, `exchange`, `currency`, `beta`, `marketCap`, `peRatioTtm`, `forwardPe`, `priceToBookTtm`, `priceToSalesTtm`, `epsTtm`, `revenue` + `revenueDate`, `netIncome` + `netIncomeDate`, `dividendYieldTtm`, `annualDividend` (Summe der dividend-Eintraege aus den letzten 365 Tagen), `payoutRatioTtm`, `debtToEquityTtm`, `returnOnEquityTtm`. None-Werte werden vor Serialisierung gestrippt damit das UI keine `—`-Fluten erzeugt. `/api/research/{symbol}` reicht das als `fundamentalsDetail` durch (leeres Dict fuer Crypto und unkonfiguriertes FMP). Frontend `FundamentalsDetailSection` rendert vier Sub-Sektionen (Identifiers, Valuation, Earnings & balance sheet, Dividend) als responsive KPI-Grid mit jeweils 2/3/4 Spalten je Breakpoint. Werte werden lokalisiert formatiert: Pct mit `%`-Suffix und zwei Nachkommastellen, Ratios mit ein bis zwei Nachkommastellen, Money via `formatLarge`-Helper oder vollformatiert mit aktueller Currency. Fiscal-Year-Datum fuer Revenue/NetIncome als kleiner Hint unter der Zahl. Neue Helpers `_safe_int` und `_sum_trailing_dividends` in `fmp_service.py`. i18n DE+EN: Top-Level `analysis.fundamentalsDetail.*` mit 24 Strings; DE nutzt Umlaute (Boerse, Waehrung) im Einklang mit dem bestehenden UI-Stil. Help-Topic `analysis.md` (+ `analysis.de.md`) um eine neue Bullet ergaenzt. 224 Unit-Tests OK (vorher 216 + 5 `IsinToWknTests` + 3 `NormalizedFundamentalsDetailTests`: SAP-WKN-Extraktion, alphanumerische WKN (adidas), Lowercase-Normalisierung, Non-DE-ISIN-Reject, Aggregation-Roundtrip, None-Stripping, unconfigured-noop). API+UI-Regression weiter gruen.
- Welle 15b Dashboard-KPIs klickbar + Hilfe-Topics auf DE am 2026-05-12: Vier Top-Stats (Watchlists, Tracked-Symbols, Alert-Rules, Open-Events) und fuenf Provider-Coverage Mini-Karten (Live/Partial/Unavailable/Research/Movers) tragen jetzt `<Link>`-Wrapper mit Hover-Highlight (`hover:border-bergt-green/60`); Drilldown-Targets sind `/watchlists`, `/alerts` und `/scanner`. i18n-Bundles um `dashboard.stats.*Aria` (Aria-Labels) und `dashboard.providerCoverage.*` (lokalisierter Sektionstitel + Mini-Labels) erweitert. `docs_service` um Locale-Parameter erweitert: `list_topics(locale)` und `get_topic(slug, locale)` resolven `<slug>.<locale>.md` → `<slug>.md` (Fallback). Locale-Variant-Files (`*.de.md`) erscheinen NICHT als separater Topic-Slug. Endpoints `/api/docs/topics?lang=` und `/api/docs/{slug}?lang=` reichen das durch. `HelpDrawer` und `DocsPage` leiten den Lang-Param aus `i18n.language` ab und invalidieren ihren Topic-Cache bei Sprachwechsel. 13 DE-Topics geschrieben (admin, alerts, analysis, auto-execution, dashboard, data-quality, discover, getting-started, mobile, news, paper-trading, scanner, settings, watchlists) — DE-Stil ohne Umlaute (ae/oe/ue/ss); Code-Identifier (`auto_execution_events`, `paper_trading.place_order`, Endpoint-Pfade) und Finanz-Akronyme (VADER, AUC, Brier, FOMC, CPI, 10-K, 8-K, ATM-IV, P(UP)) bleiben international. 216 Unit-Tests OK (vorher 210 + 6 in `DocsServiceLocaleTests`: DE-Title, DE-Fallback-auf-EN, Browser-Form-Normalisierung `de-DE`, unsupported-Locale-Fallback, Locale-Variant-Files-keine-Standalone-Topics). API+UI-Regression weiter gruen (ui_admin best-effort wie dokumentiert).
- AGENTS.md am 2026-05-10 ins Repo aufgenommen (`chore/agents-md` Fast-Forward auf main): Kopie der globalen Agent-Constitution unter `/root/agent-baseline/.claude/AGENTS.md`. Im Repo nutzbar als Projektreferenz; bricht keine bestehenden Pfade. GitHub Actions `ci`, `publish`, `codeql` gruen.
- Phase 4e-paper Auto-Paper-Trading-Loop am 2026-05-08: neuer `mode`-Schalter (`paper`|`live`, Default `paper`) via Alembic `0007_add_auto_execution_mode` (revision `c4d5e6f7a8b9`). Background-Task `auto_execution_paper_loop_task` (15min default) iteriert ueber alle User mit `enabled=true AND mode=paper`, ruft fuer jedes Watchlist-Symbol `evaluate_proposal_from_prediction` (qty = floor(maxPositionSize / entry), confidence>=0.6), bei allowed=true `paper_trading.place_order` mit `source="auto-execution-paper"`. Hard-Cap 3 Trades/User/Loop. KEIN Alpaca-Call — pure Paper-Buchung. Frontend Mode-Radio-Buttons mit Live-Confirmation-Modal; API auditiert `auto_execution.live_mode_enabled` separat beim mode-Flip. **Wichtig:** Alpaca ist NICHT der echte Broker des Operators — Alpaca diente nur als Quotes-/Paper-Source. Der `mode=live`-Pfad ist Code-Path-Test gegen Alpaca-Live-Tier, NICHT der produktive Pfad fuer echte Geld-Trades. Echter Broker-Adapter ist als Phase 5 (oder Welle 15) markiert; konkrete Broker-Wahl steht beim User aus. 208 Unit-Tests OK (vorher 203 + 5 evaluate_from_prediction). API+UI-Regression weiter gruen.
 vier Sub-Schnitte (4a Risk-Modell+Limits+Audit-Tabellen, 4b Manuelle-Freigabe+Halt-Trigger-Wiring, 4c Not-Aus-Endpoint, 4d Frontend+Tests). Alembic-Migration `0006_add_auto_execution` mit `auto_execution_limits` (1:1, Default-Row mit `enabled=false`/max_position=$500/max_daily_loss=$200/max_open=5/max_beta=2.0/allowlist leer) und `auto_execution_events` (Audit-Log mit Status proposed/accepted/rejected/executed/failed/halted). Neuer `app/auto_execution.py` Service mit `evaluate_proposal` (5 Risk-Gates: master/asset-class/position-size/daily-loss/open-positions + 4 Halt-Trigger: FOMC<24h via FRED, 8-K<7d via FMP, Yield-Curve-Inverted nur fuer Stock/ETF, Symbol-Beta>Limit + Net-Yield-Gate) und `halt_all_for_user`. Vier neue Endpoints: `GET/PUT /api/auto-execution/limits`, `GET /api/auto-execution/events`, `POST /api/auto-execution/proposals/evaluate` (Dry-Run inkl. live-fetched FRED+FMP+Sektor+NetYield), `POST /api/auto-execution/halt`. Backup/Export/Import-Coverage. Frontend-Page `/auto-execution` mit Limits-Form, Master-Toggle mit Confirmation-Modal beim Aktivieren, prominenter Halt-Button, Event-Log. Layout-Nav-Eintrag "Auto-Execution". Help-Topic `auto-execution.md` mit page-frontmatter. i18n DE+EN. 203 Unit-Tests OK (vorher 188 + 15 auto_execution). Defaults bleiben durchgaengig OFF — kein automatischer Trade-Loop ist gleichzeitig scharf geschaltet, das ist explizit Folgewelle.
- Welle 14 Datenbasis-Tiefe am 2026-05-08: drei neue Backend-Services + drei neue Frontend-Sections + ein Dashboard-Widget. (a) `fmp_service.normalized_sec_filings(symbol)` aggregiert EDGAR-Filings, klassifiziert in annual/quarterly/material/proxy/offering/insider/other, liefert `lastAnnual`/`lastQuarterly`/`lastMaterial` Snapshots; `/api/research/{symbol}` ergaenzt um `secFilings`; Frontend `SecFilingsSection` mit Direktlinks. (b) Neuer `app/fred_service.py` mit Cache 60min/30min, aggregiert Treasury 2Y/10Y/Spread (mit `spreadInverted`-Flag), Commodities (WTI/Gold), Policy (FedFunds/CPI/NFP/Unemployment), und Upcoming-Releases fuer 5 IDs (CPI/NFP/FOMC/GDP/PCE); Endpoint `GET /api/macro/calendar` (global); Frontend `MacroCalendarCard` im Dashboard. (c) Neuer `app/sector_service.py` mit yfinance-basiertem Sector-ETF-Match (XLK/XLF/XLE/XLV/XLY/XLP/XLI/XLB/XLU/XLRE/XLC), Trailing-Returns 1m/3m/6m vs SPY/QQQ/IWM/Sector mit Alpha-Berechnung, Korrelation + Beta vs SPY 90-Tage (numpy-frei via Cov/Var); `/api/research/{symbol}` ergaenzt um `sectorContext`; Frontend `SectorStrengthSection`. i18n-Bundles ergaenzt um `analysis.sectorStrength.*`, `analysis.secFilings.*` (inkl. 7 EDGAR-Kategorien), Top-Level `macroCalendar.*`. Help-Topics `analysis.md` + `dashboard.md` erweitert. 188 Unit-Tests OK (vorher 172 + 3 SEC-Filings + 8 FRED + 5 Sektor). Damit ist die geplante Vor-Phase-4-Datenbasis abgeschlossen — Phase 4 Auto-Execution kann mit auditierbaren Halt-Triggern (FOMC<24h, Yield-Curve invertiert, 8-K-Window, Sektor-Beta>Limit) angegangen werden.
- Welle 12 DE-Uebersetzungen am 2026-05-08: i18n-Bundles `src/frontend/src/i18n/{de,en}.json` um `analysis.{modelPerformance,dataQuality,researchSignals,earningsCalls,macroContext,cryptoMetrics,socialSentiment,optionsFlow}`, `news.*`, `discover.*`, `docs.*`, `pwa.*` ergaenzt. `NewsHubPage`, `DiscoverPage`, `HelpDrawer`, `DocsPage`, `PwaUpdatePrompt` komplett auf `t()`; `AnalysisPage` mit allen acht Sub-Sections (Title + Subtitle + Key-Labels) uebersetzt. Tabellen-Header und finanztechnische Labels (VADER, AUC, Brier, P/C-Ratio, Strike, $TICKER) bewusst international Englisch — auditierbare Vergleichbarkeit ueber Sprachen hinweg. Frontend-Bundle 132 KB gz (vorher 128 KB; +4 KB durch zusaetzliche String-Tables). 172 Unit-Tests OK. API+UI-Regression weiter gruen mit `ui_pwa_manifest ok`. Welle 12 schliesst die 12-Wellen-Sequenz ab; naechster grosser Block ist entweder Phase 4 Auto-Execution oder Welle 14 Datenbasis-Tiefe (SEC-Filings, FRED, Sektor-Relativstaerke).
- Welle 11 Phase A Android via PWA am 2026-05-08: Vite-PWA-Plugin integriert mit kompletter Manifest-Konfig (name, theme #22c55e, display=standalone, SVG-Icon any+maskable). Workbox-Caching: NetworkFirst fuer `/api/*` (5s timeout, 1h fallback), CacheFirst fuer Static-Assets (7d), NavigateFallback `index.html` mit Denylist fuer `/api`+`/ws`. SVG-Icon `public/icon.svg` mit Brand-Color-Logo. `index.html` mit theme-color, apple-touch-icon, viewport-fit=cover. `PwaUpdatePrompt`-Component mit dynamic-import von `virtual:pwa-register` (Bundle-Impact nur bei SW-Support). Help-Topic `mobile.md`. UI-Regression neuer Step `ui_pwa_manifest ok`. Initial-Bundle 128 KB gz, neue Chunks `virtual_pwa-register` 0.56 KB + `workbox-window` 2.36 KB lazy. 172 Unit-Tests OK. Phase B (Capacitor + Biometric) zurueckgestellt.
- Welle 10 Security-Welle am 2026-05-08: (a) `aquasecurity/trivy-action` als zweistufiger Image-Vulnerability-Scan in `ci.yml` + `publish.yml` (HIGH+CRITICAL, ignore-unfixed, soft-fail mit kuratierter `.trivyignore`); (b) Strict Security-Header in `frontend.nginx.conf` (HSTS, X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy, CSP fuer Vite/React); (c) `_read_admin_upload_json`-Helper validiert Content-Type-Allowlist und enforced Size-Cap `ADMIN_UPLOAD_MAX_BYTES` (default 50MB) auf beiden Admin-Imports — HTTP 400/413 statt OOM-Risiko; (d) Per-Account-Login-Rate-Limit als zweiter Bucket (10/15min) zusaetzlich zum Per-IP-Bucket schliesst die IP-Rotation-Luecke. 172 Unit-Tests OK (vorher 167 + 5 admin_upload + 1 skipped). API+UI-Regression weiter gruen.
- Welle 9b Discovery-Engine am 2026-05-08: `/discover`-Page mit drei orthogonalen Stock-Discovery-Views: Trending nach News-Mention-Volume + Sentiment-Burst (aus 9a-Feed aggregiert mit per-window-projection-Math fuer ehrlichen Trend-Pct), Top-Movers (FMP `/stock_market/{gainers|losers|actives}`), Insider-Clusters (FMP v4 `/insider-trading-rss-feed` aggregiert mit Set-basiertem unique-Insider-Count, Buy/Sell-Direction-Klassifikation, Net-Value, Filtering >=3 Insiders + 90-Tage-Cutoff). `app/discovery_service.py` mit Modul-Cache 15 min. Endpoint `GET /api/discover`. Layout-Nav-Eintrag "Discover"/"Entdecken". Help-Topic `discover.md`. 167 Unit-Tests OK (vorher 161 + 6 discovery + 1 skipped). API+UI-Regression weiter gruen.
- Data-Source-Transparency am 2026-05-08 (Phase-4-Vorbedingung): `app/data_quality_service.py` mappt pro Symbol jedes Datenfeld auf FULL/PARTIAL/FALLBACK/MISSING + Provider; `_overall_confidence` reduziert auf high/medium/low (≥60% FULL → high, ≥60% FULL+PARTIAL → medium, sonst low); statisch-auditierbare Upgrade-Hints (FMP Starter $14/mo, Alpha Vantage Premium $50/mo, Polygon.io Stocks $29/mo, Reddit OAuth free). Endpoints `GET /api/research/{symbol}/data-quality` + admin-only `GET /api/admin/data-sources`. Frontend `DataQualitySection` oben auf `/analysis/<symbol>` mit Overall-Pille + Per-Field-Grid + Upgrade-Hint-Block. PredictionCard `Data: high|medium|low`-Badge unter ML-Direction. Admin `DataSourcesSection` als Tabelle mit Monthly-Cost-Footer. Help-Topic `data-quality.md`. 161 Unit-Tests OK (vorher 156 + 5 data_quality + 1 skipped). API+UI-Regression weiter gruen.
- News-Hub Welle 9a am 2026-05-08: globaler Multi-Source-News-Feed unter `/news`. `app/news_hub_service.py` aggregiert FMP `/stock_news` (ohne Symbol-Filter), Alpha-Vantage `NEWS_SENTIMENT` mit Topics, plus RSS-Feeds (boerse.de, ariva.de, Reuters Markets via `feedparser==6.0.11`). Symbol-Extraction via $TICKER-Regex; Deduplizierung by URL; Sortierung newest-first ueber geparste Datetimes (mehrere Format inkl. RFC-822 fuer RSS); Cache 5 min. Endpoint `GET /api/news/feed?limit&offset&sources&sentiment&since&symbol` mit pagination. Frontend `NewsHubPage` mit 4-Spalten-Filter-Bar und klickbaren Symbol-Chips als Discovery-Pfad. Layout-Nav-Eintrag "News". Help-Topic `news.md`. 156 Unit-Tests OK (vorher 151 + 5 news_hub + 1 skipped).
- In-App-Hilfe + Online-Doku am 2026-05-08: Markdown-Quellen unter `docs/inapp/<topic>.md`, Endpoints `GET /api/docs/topics` (mit pageMap fuer kontextuelle Hilfe) und `GET /api/docs/{slug}` (no-auth, Path-Escape-defensiv). Frontend `HelpDrawer` als Slide-Over im Layout-Header (?-Button), kontextuelles Topic-Lookup via URL-Pfad-Prefix-Resolver. `/docs` und `/docs/:slug` Routes mit Side-Nav + Markdown-Content. `HelpMarkdown`-Component ist lazy-importiert via React.lazy damit react-markdown + remark-gfm nur beim ersten Drawer-Open in den Bundle landen (Initial-Bundle bleibt 125 KB gz, HelpMarkdown-Chunk 47 KB gz lazy). 9 Initial-Topics (getting-started, dashboard, watchlists, scanner, analysis, alerts, paper-trading, settings, admin). 151 Unit-Tests OK (vorher 146 + 5 docs_service + 1 skipped). API+UI-Regression weiter gruen.
- Audit-Log + Daily-Re-Training-Task am 2026-05-08 (Phase-4-Vorbedingung): Alembic-Migration `0005_add_audit_events` (revision `f1a8c2d6e7b9`) erzeugt `audit_events`-Tabelle mit user_id/action/resource_type/created_at-Indizes. `app/audit_service.py` mit `log_event`-Helper, identifizierende Werte (E-Mail, IP, User-Agent) als 16-char-SHA256-Fingerprint, Action-Vokabular als Konstanten, defensiv gegen DB-Failure. Hooks an 14 sensitiven Endpoints (auth.login success/failed-with-reason, register, password_reset_confirm, mfa_enable/disable, alpaca/portfolio settings, paper_order place/cancel/gate_rejected, backup create/restore/export/import, admin user_create/password_reset/mfa_reset/toggle_active). Admin-Endpoint `GET /api/admin/audit-events?user_id=&action=&limit=&offset=` mit pagination. BackupService deckt audit_events mit. Daily-Background-Task `ml_retrain_task` (1h Intervall) iteriert ueber Watchlist-Symbol-Union + persistierte Modelle, refreshed stale Predictors proaktiv. 146 Unit-Tests OK (vorher 140 + 6 audit + 1 skipped).
- ML-Persistenz + Backtest-Framework am 2026-05-08 (Phase-4-Vorbedingung): `app/ml_persistence.py` persistiert pro Symbol einen XGBoost-Predictor unter `state/runtime/data/ml_models/<SYMBOL>.json` plus `<SYMBOL>.meta.json`. `MarketDataService._get_or_train_predictor` checkt Memory (1h) → Disk (24h) → Train+Persist. `app/backtest_service.run_backtest` macht Walk-Forward-Loop und liefert Accuracy/AUC/Brier-Score/Strategy-vs-Buy-Hold/10-Bucket-Reliability-Tabelle. Endpoint `GET /api/research/{symbol}/backtest`. Frontend `ModelPerformanceSection` rendert Direction-Accuracy + AUC + Brier, Strategy-vs-Hold-Edge mit Color-Coding, Reliability-Tabelle mit Drift-Color-Coding fuer Calibration-Lesbarkeit. 140 Unit-Tests OK (vorher 130 + 5 ml_persistence + 4 backtest + 1 skipped). API+UI-Regression weiter gruen.
- Phase 3 Paper-Trading abgeschlossen am 2026-05-08 (vierter Schnitt): dynamische Slippage skaliert mit Position-Size relativ zum 20-Tage-Volumen (`_resolve_slippage_pct(asset_class, qty, avg_daily_volume)`, gecappt bei `MAX_SLIPPAGE_PCT=1.0`). Asset-spezifische Fee-Multipliers (`FEE_MULTIPLIER_BY_ASSET_CLASS` Stock 1.0, ETF 1.0, Crypto 5.0) skalieren User-Settings pro Asset-Klasse. `evaluate_net_yield_gate` und `fee_breakdown` nutzen denselben Multiplier (kein Drift zwischen Gate-Entscheidung und Fill-Cost). `MarketDataService.get_avg_daily_volume(symbol)` als Volume-Provider (Alpaca → yfinance-Fallback). 130 Unit-Tests OK (vorher 126 + 4 Phase-3-Restverfeinerung). Phase 4 Auto-Execution kann jetzt nach Risk-Modell + Limits + Not-Aus angegangen werden.
- Datenbasis-Welle 8 am 2026-05-08: Neuer Adapter `app/twelve_data_service.py` als dritter Provider-Fallback hinter yfinance + FMP, primaer fuer Non-US-Symbols (`SAP.DE`, `BMW.DE`, `LVMH.PA`, `BARC.LON`, `7203.T`). `normalized_ticker_info` mappt Twelve-Data-Profile/Statistics/Quote auf yfinance-kompatible Felder. Defensiv gegen Twelve-Data-typische `{"status":"error"}`-Responses. Optional `TWELVE_DATA_API_KEY`. `MarketDataService.get_ticker_info` chained yfinance → FMP → Twelve Data ueber `_has_meaningful`-Check; Twelve-Data-Call nur wenn beide Vorgaenger leer. `rate_limit.py` ergaenzt um Provider-Default `twelve_data` 0.13 req/s. 126 Unit-Tests OK (vorher 118 + 6 Twelve-Data + 2 Chain). API+UI-Regression weiter gruen.
- Datenbasis-Welle 7 am 2026-05-08: FinBERT (`ProsusAI/finbert`) als optionaler Premium-Sentiment-Provider ueber `SENTIMENT_PROVIDER=finbert`. Schwere Dependency-Schicht (transformers + torch) liegt in neuem `src/backend/requirements-finbert.txt`, Default-Container bleibt schmal. `app/sentiment.py` mit Lazy-Singleton-Pipeline (lockguarded), Mapping FinBERT-Label/Score auf [-1, 1], transparenter Fallback auf VADER bei `ImportError` oder Pipeline-Load-Failure (Disabled-Flag verhindert Retry-Storm). 118 Unit-Tests OK (vorher 113 + 5 FinBERT). API+UI-Regression weiter gruen.
- Docker-Hub-Release `2026.05.08-1` am 2026-05-08 erfolgreich gepusht und upgrade-/restore-rehearsal-validiert:
  - `dbergt/trading-bot-backend:2026.05.08-1` -> `sha256:bbbe4628833921abb880c4ad336ab892445a0254a27bdd066689cd16e4997d13`
  - `dbergt/trading-bot-frontend:2026.05.08-1` -> `sha256:e4c09ffa32194500a1c59d3b44bf1bf3ae4d33296d5531a0f9cff1d88aef9b88`
  - Upgrade-Record: `state/runtime/deployments/deployment-20260508T101330Z.env`
  - Pre-Upgrade-Postgres-Dump + App-Snapshot enthalten paper_orders/paper_transactions (Migration 0004 sauber gestempelt + upgegradet)
- Datenbasis-Welle 6 am 2026-05-08: `app/options_flow_service.py` zieht yfinance `Ticker.option_chain` fuer das nearest-expiry mit >=7 Tagen Restlaufzeit. Aggregiert pro Symbol Put/Call-Volume-Ratio, Put/Call-OI-Ratio, Average-IV ATM (±5%-Band um Last-Close) und Top-3-Strikes Calls + Puts nach Volume. Skew-Klassifikation bullish/bearish/neutral mit P/C-Schwellen 0.7/1.2. Cache 60 min pro Symbol; Crypto skipped. Defensive Failure-Pfade: leere Chains, yfinance-Errors, Rate-Limit-Skip alle empty-payload. `/api/research/{symbol}` ergaenzt um `optionsFlow`. Frontend `OptionsFlowSection` mit Header (Expiry + Spot + Skew-Pille), drei KPI-Cards, zwei Strike-Tabellen. 113 Unit-Tests OK (vorher 105 + 8 Options-Flow). API-Regression bestaetigt `optionsFlow`-Schema. UI-Regression diesmal komplett gruen — auch `ui_admin ok`.
- Datenbasis-Welle 5 am 2026-05-08: `app/fmp_service.py` ergaenzt um `get_earnings_transcripts(symbol, years_back=2)` (FMP v4-Batch-Endpoint, sortiert by date desc) und `normalized_earnings_calls(symbol, limit=4)` mit VADER-Aggregation pro Quartal. Sentence-Tokenisierung via Regex extrahiert je Quartal die hoechst-positive und hoechst-negative Sentence (>=30 Zeichen, gekuerzt 280 Zeichen) plus VADER-Score. `/api/research/{symbol}` ergaenzt um `earningsCalls` (immer Liste). Frontend `EarningsCallsSection` rendert pro Quartal eine Card mit Header (Q+Y+Date), VADER-Label-Pille (bullish/bearish/neutral farbcodiert) und beiden Quotes als Blockquote mit farbig markiertem Linkrand. 105 Unit-Tests OK (vorher 103 + 2 Earnings-Call). API-Regression bestaetigt `earningsCalls`-Liste-Check, UI-Regression weiter komplett gruen.
- Datenbasis-Welle 4 am 2026-05-08: Retail-Sentiment-Adapter `app/social_sentiment_service.py` zieht StockTwits (`/streams/symbol/{SYMBOL}.json`, kein Auth) und Reddit (offentliche `search.json`, kein PRAW; User-Agent `trading-bot-v2/1.0`). StockTwits zaehlt self-tagged Bullish/Bearish/Neutral und mittelt VADER ueber die Bodys; Reddit prueft Stock- (wallstreetbets/stocks/investing) oder Crypto-Subreddits (CryptoCurrency/Bitcoin/ethereum), liefert 24h/7d-Mention-Counts und Mention-Trend gegen 6-Tage-Baseline. Combined-Sentiment weighted-averaged ueber Message-Counts. Modul-Cache 30 min, Rate-Limit-Defaults `stocktwits` 1 req/s und `reddit` 0.5 req/s. `/api/research/{symbol}` ergaenzt um `socialSentiment`. Frontend `SocialSentimentSection` mit drei KPI-Cards plus Top-Posts-Tabellen (Reddit-Permalinks klickbar). 103 Unit-Tests OK (vorher 96 + 6 Social-Sentiment + 1 Edge). API-Regression bestaetigt `research signals + macro context + social sentiment shape ok`. UI-Regression weiter gruen mit `ui_macro_context ok`.
- Datenbasis-Welle 3 am 2026-05-08: Neuer Adapter `app/coingecko_service.py` mit Top-30-Coin-ID-Static-Map plus `/coins/markets`-Fallback. `get_coin_metrics(symbol)` liefert Marktkap-Rang/-Wert, 24h Total-Volume, ATH/ATL mit Distance, Community (Twitter/Reddit) und Developer-Daten (Stars/Forks/Commits 4 Wochen). `get_fear_greed_index()` zieht den Crypto-Fear-and-Greed ueber `alternative.me`. Modul-Cache (5 min Coin-Metriken, 30 min Fear-Greed), optionaler `COINGECKO_API_KEY`-Pro-Schalter. `rate_limit.py` ergaenzt um Provider-Defaults `coingecko` (0.5 req/s) und `fear_greed` (0.05 req/s). `/api/research/{symbol}` ergaenzt um `cryptoMetrics` und `fearGreedIndex`. Frontend `CryptoMetricsSection` und Fear-Greed-Card in `MacroContextSection`. Phase 3 letzter Punkt: asset-spezifische Slippage (`SLIPPAGE_PCT_BY_ASSET_CLASS` mit Stock 0.1%, ETF 0.05%, Crypto 0.3%); `_try_fill` nimmt `asset_class`, `place_order`/`dispatch_pending_orders` einen optionalen `asset_class_resolver`. 96 Unit-Tests OK (vorher 87 + 7 CoinGecko + 2 Slippage). API-Regression bestaetigt `crypto research + crypto metrics shape ok`. UI-Regression vollstaendig gruen (auch `ui_macro_context ok` diesmal).
- Datenbasis-Welle 2 am 2026-05-08: Sentiment-Pfad migriert von TextBlob auf VADER (`vaderSentiment==3.3.2`); `app/sentiment.py` neu geschrieben mit Modul-Singleton-Analyzer (lazy + lockguarded), `analyze_sentiment_basic` liefert Compound in [-1, 1]; `analyze_news` haelt die alte Schnittstelle bei. Schwellen ueber `SENTIMENT_BULLISH_THRESHOLD`/`SENTIMENT_BEARISH_THRESHOLD` tunbar (Default ±0.1). `SENTIMENT_PROVIDER=finbert`-Schalter dokumentiert; aktueller Fallback auf VADER mit Warn-Log. Neuer Test `tests/test_sentiment.py` (4 Tests).
- Phase 3 zweiter Schnitt am 2026-05-08: pending Limit-Orders fuellen sich ueber Background-Task `paper_order_fill_task` periodisch gegen `MarketDataService.get_latest_close` (Default 180s Intervall, 60s Initial-Delay; per-Symbol-Cache pro Zyklus). Paper-Trades als Marker im StockChart auf `/analysis/<symbol>` (Buy/Sell-Pfeile mit Qty + Preis). Recommendation-Card bekommt "Place paper order at this target"-Link, der `/paper-trading` mit symbol/side/limitPrice/targetPrice/source-Parametern vorbelegt; PaperTradingPage liest die Params via `useSearchParams`. 87 Unit-Tests OK (vorher 81 + 2 Dispatch + 4 Sentiment).
- Datenbasis-Welle 1 am 2026-05-08 ausgeliefert: FMP-Service erweitert um Insider-Trading (FMP v4), Institutional-Holder, Earnings-Surprises, Upcoming-Earnings; gebuendelt als `normalized_research_signals` mit 90-Tage-Insider-Summary (Buys/Sells in Shares + Net-Value), Top-10-Institutional-Holdings, Earnings-Beat-Quote ueber max. 8 Quartale, naechstem Earnings-Datum + Days-Until. Neuer Adapter `app/macro_service.py` zieht VIX/10Y-Yield/DXY ueber yfinance, gecacht (5 min TTL). `/api/research/{symbol}` reicht beide Bloecke unter `researchSignals` und `macroContext` durch. Frontend AnalysisPage rendert ResearchSignalsSection (KPI-Karten Insider-Net-Value/Beat-Rate/Next-Earnings + drei Tabellen Insider/Institutional/Beats) und MacroContextSection (3-Karten-Grid mit Change-Pct). 81 Unit-Tests OK (vorher 74 + 7 neue: 3 FMP, 4 Macro). API-Regression bestaetigt `research signals + macro context shape ok`. UI-Regression `ui_macro_context` als best-effort-Schritt eingebaut (yfinance-Konnektivitaet im isolierten Stack volatil, parallel zum Admin-Skip-Pattern).
- Phase 3 Paper-Trading-Erststand am 2026-05-08 ausgeliefert: Alembic-Migration `0004_add_paper_trading` (`c8e2a1b9d4f3`) erzeugt `paper_orders` und `paper_transactions`; `app/paper_trading.py` implementiert Order-Lifecycle (Market-Slippage 0.1% adverse, Limit-Fill-Logik, weighted-average Cost-Basis, realized PnL nach Brokergebuehren und Kapitalertragssteuer) und Net-Yield-Gate auf Order-Annahme (gated nur wenn `target_price` gesetzt; nutzt denselben Fees+Tax-Rechenweg wie `_enrich_with_yield_model`). `MarketDataService.get_latest_close` als Best-Effort-Quelle (Alpaca -> Alpha Vantage -> yfinance). Endpunkte `POST/GET/DELETE /api/paper-trading/orders` und `GET /api/paper-trading/transactions|positions|summary` mit `Depends(get_current_user)` und `user_id`-Filter. Backup/Export/Import deckt `paper_orders` und `paper_transactions` mit ab. Frontend-Page `/paper-trading` (DE/EN, im Layout-Nav verlinkt) mit Place-Order-Form (symbol/side/qty/limit/target/source) und Tab-Navigation (Open Orders / Trade-Journal / Positions / Summary); Journal lueckenlos chronologisch mit nominaler PnL, %-PnL relativ zum konsumierten Cost-Basis, summierter Gebuehren- und Steuerlast je Position und in Summe. 74 Unit-Tests OK (vorher 64 + 10 Paper-Trading-Tests). API-Regression bestaetigt `paper trading settings ok`, `paper trading net yield gate reject ok`, `paper trading order placed ok`, `paper trading orders/transactions/positions/summary list ok`, `paper trading cancel ok` plus Backup/Export-Coverage. UI-Regression bestaetigt `ui_paper_trading ok`. Frontend-Build clean (124 KB gzipped).
- Frontend-Production laeuft jetzt vollstaendig ueber den React-Quellstand `src/frontend/`. `ops/docker/frontend.Dockerfile` ist Multi-Stage (node:22-alpine fuer Vite-Build, nginx:1.29-alpine fuer Serve); `src/frontend-dist/` ist entfernt. `tests/run-ui-regression.mjs` ist komplett auf React-Selektoren umgeschrieben; AdminPage-Step ist best-effort wegen React.lazy-Suspense-Edge-Case in Headless-CI.
- UX-Direktive in `docs/admin/project-plan.md` Sektion "UX-Direktive" verankert (uebersichtlich + einfach, Menues/Dropdowns, DE/EN, source-traceable Empfehlungen, Werte mit Einheit + Vorzeichen + Vergleichswert, Onboarding sichtbar im Dashboard, lueckenloses chronologisches Trade-Journal als Phase-3-Pflichtinhalt).
- i18n-Base-Layer eingebaut: `i18next` + `react-i18next`, `src/frontend/src/i18n/{en,de}.json`, `LanguageToggle` im Login + im Layout-Header; Layout/LoginPage/DashboardPage/OnboardingCard/OnboardingPage komplett uebersetzt; Persistenz per localStorage.
- Phase-2-Tiefe ausgeliefert: FMP `cash-flow-statement`, `balance-sheet-statement`, `rating`, `analyst-estimates` ueber `fmp_service.normalized_research_depth`; `/api/research/{symbol}` reicht `researchDepth` durch; AnalysisPage rendert ResearchDepthSection mit Rating, Forward-Estimates, FCF/CapEx, Debt/Equity.
- Produktvision (Stand 2026-05-07) ist in `docs/admin/project-plan.md` Sektion "Produktvision" verbindlich verankert: kombinierte Signal-Quellen (Fundamentals + News + Trend + Technical + AI), explizite Wahrscheinlichkeiten, Net-Yield-Gate (Brokergebuehren + Kapitalertragssteuer netto >= `min_target_yield`), First-Login-Wizard fuer alle Pflichtwerte, Dashboard-Karte mit Setup-Fortschritt, Erklaerbarkeit als Kern (Kategorie + Feature)
- Phase 2 mit sechs Wellen abgeschlossen: Chart-Overlays (lightweight-charts v5 + VWAP), Earnings/Dividenden/Splits ueber FMP, Volume-Profile mit Point-of-Control, KI-Erklaerbarkeit (xgboost_pred_contribs + Kategorie-Roll-up + P(UP)/P(DOWN)), Net-Yield-Gate mit Schema-Erweiterung (Alembic-Migration `0003_add_user_tax_rates`/`a3c1d4f5e6b7`), Auto-Support/Resistance mit Swing-Pivot-Clustering und Stärke-basierter Visualisierung; 63 Unit-Tests OK; API-Regression gruen
- Onboarding-Wizard `/onboarding` mit vier Schritten (MFA, Alpaca, Trading-Defaults, Steuern); Dashboard zeigt `OnboardingCard` mit Fortschritt; Register-Flow leitet neue Nutzer direkt in den Wizard
- Phase 1 ist Provider-seitig geschlossen: zentraler Token-Bucket-Rate-Limiter (`app/rate_limit.py`) fuer Alpha Vantage, FMP und yfinance; neuer FMP-Adapter (`app/fmp_service.py`) mit Profile/Key-Metrics/Ratios/ETF-Holdings/News; `MarketDataService.get_ticker_info` chained yfinance -> FMP fuer Stocks-Fundamentals; `MarketDataService.get_market_news` chained Alpaca -> FMP fuer News-Sentiment; `tests/run-fmp-live-smoke.sh` analog zum Alpha-Vantage-Smoke; 48 Unit-Tests OK; API-Regression gruen
- Alembic-Migrationen auf Production-Cross-Version-Pfad korrekt: `0001_initial_schema` (`b97b927c8690`) deckt nur die 8 Tabellen aus v2026.05.07-1 ab; `0002_add_alert_rules_and_events` (`d939f2abcdc2`) ergaenzt die zwei neuen Tabellen; `init_db` differenziert zwischen Fresh-DB, Pre-Alembic-Baseline (8 Tabellen, stamp Baseline + upgrade) und Pre-Alembic-Full (10 Tabellen, stamp Head); echte Postgres-Cross-Version-Verifikation mit `dbergt/trading-bot-backend:2026.05.07-1` -> aktueller Stand erfolgreich
- Pre-commit-Hook wird ab jetzt automatisch durch `ops/automation/build.sh` aktiviert, da `core.hooksPath` per-repo ist und nicht via `git clone` propagiert
- Frontend-Quellstand-Wiederaufbau ist substanziell vorangekommen: `src/frontend/` enthaelt jetzt Login, Register, ForgotPassword, ResetPassword, Dashboard mit Tracked-Assets/Provider-Coverage/News-Ticker, Watchlists mit Item-CRUD und Tag-Pflege, Scanner mit Provider-Status-Pills, AnalysisPage mit Provider-Research/Fundamentals/Holdings/News, AlertsPage mit Alert-Rule-CRUD plus Event-Acknowledge, SettingsPage (Alpaca/Portfolio/MFA), AdminPage (Users/Backups/Export); Vite-Build clean (330 KB / 98 KB gzipped); CI-Stages `Install frontend source dependencies` + `Build frontend source` als Bitrot-Gate aktiv; Production laeuft unveraendert ueber `src/frontend-dist`-Bundle, Swap wartet bewusst auf koordinierte UI-Regression-Umschrift
- Schema-Lifecycle laeuft ab jetzt ueber Alembic; initiale Migration `0001_initial_schema` (`16389c42c243`) deckt alle 10 Tabellen inkl. Indizes ab; `init_db` stempelt Pre-Alembic-Deployments automatisch auf head; `tests/test_alembic_init.py` deckt Fresh-DB- und Pre-Alembic-Stamp-Pfad in Subprocess-isolierten Szenarien ab; `bash ops/automation/test.sh` -> 31 Tests OK; `SKIP_BUILD=1 bash tests/run-api-regression.sh` -> erfolgreich gegen alembic-managed Schema
- Persistente Alert-Domaene `alert_rules`/`alert_events` lokal umgesetzt mit Regeltypen `provider_move`, `news_sentiment`, `signal_direction`, `tag_priority`; Endpunkte `/api/alerts/rules`, `/api/alerts`, `/api/alerts/events`, `/api/alerts/events/{id}/ack`; Backup/Export/Import decken Regeln und Events mit ab; API-Regression prueft Anlage, Auswertung, Acknowledge und Backup-/Export-Coverage erfolgreich
- `CLAUDE.md` als Repo-Orientierung sowie `.githooks/pre-commit` (aktiviert per `core.hooksPath`) gegen versehentliche Secret-Leaks (`.env.local`, `*.pem`, AWS-/GitHub-/Slack-Token, Private-Key-Bloecke, High-Entropy-Zuweisungen) sind eingebaut und mit AWS-Key- und Private-Key-Sample verifiziert
- `dbergt/trading-bot-frontend:latest` und `dbergt/trading-bot-backend:latest` sind am 2026-03-18 lokal verifiziert und aktuell
- interner Compose-Name in beiden Images: `trading-bot-v2`
- Backend-Quellcode konnte aus dem Image extrahiert werden
- Frontend liegt aktuell nur als Build-Artefakt vor
- Handover fuer naechste Sitzung liegt in `state/handover-2026-03-18.md`
- isolierter PostgreSQL-Lauf mit aktuellem Backend-Quellstand wurde erfolgreich geprueft
- Admin-Endpunkte fuer Backup-Liste, manuelles Backup, Export, Import und Backup-Download wurden im Docker-Testnetz erfolgreich geprueft
- Scanner-Leerlauf-Busy-Loop wurde behoben
- Build-Automation bereitet nun beschreibbare Bind-Mount-Verzeichnisse fuer den nicht-root Backend-Container vor
- Backup-Scheduler bricht bei Schreibfehlern nicht mehr als Task ab
- reproduzierbares Skript `tests/run-api-regression.sh` fuehrt die verifizierten API-Laufzeitproben automatisiert aus
- API-Regression prueft jetzt auch Passwort-Reset-Request, Token-Confirm und Re-Login
- Startup kann jetzt einen initialen Admin aus `INITIAL_ADMIN_EMAIL` und `INITIAL_ADMIN_PASSWORD` bootstrappen; mit `INITIAL_ADMIN_MFA_ENABLED=false` ist der erste Admin-Login ohne MFA moeglich
- Export-Pruefung der API-Regression wurde korrigiert: nach dem explizit angelegten zweiten Benutzer validiert der Test jetzt die erwarteten exportierten E-Mail-Adressen statt eines veralteten `users == 1`-Annahmefehlers
- frischer lokaler Build des aktuellen Quellstands wurde erfolgreich ausgefuehrt
- API-Regression lief nach der Testkorrektur erneut erfolgreich gegen die frisch gebauten lokalen Images
- UI-Regression lief anschliessend erneut erfolgreich gegen den frisch gebauten Compose-Stack
- ausgeliefertes Frontend-Bundle wurde um einen gezielten DOM-Patch erweitert: Dashboard-Shortcuts fuer Account Settings und User Administration tragen jetzt klar getrennte Icons/Titel, und die Alpaca-/Settings-Seite hat einen sichtbaren `Back to Dashboard`-Button
- UI-Regression prueft diese beiden Navigationsdetails jetzt explizit, behandelt bereits laufende Nicht-Admin-Stacks rollenbewusst und validiert den Admin-Shortcut im isolierten Fresh-Stack weiterhin verpflichtend; zusaetzlich ist der Lauf gegen ein Headless-Chrome-Cleanup-Race beim Entfernen des temporaren Profils gehaertet
- das rekonstruierte Frontend zeigt jetzt erste Phase-1-Produktdaten direkt im Dashboard: ein neues `Watchlist Alerts`-Panel rendert priorisierte Alert-Kandidaten aus `/api/watchlists/{id}/alerts`, und der bisher generische Footer-Ticker wird mit beobachteten Watchlist-News aus `/api/watchlists/{id}/news` gespeist
- derselbe Dashboard-Patch liefert jetzt auch echte Interaktion: Alert-Karten haben Drilldown-Aktionen fuer Analyse/Trade/News, neue High-Priority-Alerts koennen als deduplizierte Toast-Popups erscheinen, und die Watchlist-Insights werden periodisch aktualisiert
- derselbe Dashboard-Patch nutzt jetzt auch `trackedAssets` sichtbar: eine neue `Tracked Assets`-Sektion zeigt Assetklassen-Mix, Top-Tags, Exchange-/Market-Metadaten und pro Symbol Analyse-/Trade-Drilldowns direkt im Dashboard
- Watchlist-News und Tracked-Asset-Metadaten werden im UI jetzt getrennt vom langsameren Alert-Feed gecacht und bereits im Teilzustand gerendert; dadurch bleiben Ticker, Asset-Mix und Tag-Summary auch dann sichtbar, wenn `/api/watchlists/{id}/alerts` noch laeuft oder partiell spaeter eintrifft
- `tests/run-ui-regression.mjs` seedet im isolierten Stack jetzt Watchlist-Tags per API und validiert Asset-Mix, Top-Tags und Tracked-Asset-Karten erfolgreich; der komplette UI-Lauf war gegen den lokalen Build auf Alternativports `18190/18194` erfolgreich
- `MarketDataService` cached YFinance-Fundamentals und Watchlist-News jetzt mit TTL, ueberspringt Krypto-Fundamentals komplett und vermeidet im Watchlist-Alert-Pfad doppelte News-/Fundamentals-Fetches; dadurch werden ETFs/Krypto im Backend robuster behandelt und Alert-Latenz/Ratelimit-Druck sinkt
- ETF-Klassifizierung greift jetzt auch aus expliziten Watchlist-Namen wie `Vanguard S&P 500 ETF`, selbst wenn kein externer Provider gerade Metadaten liefert
- API-Regression deckt jetzt neben dem Krypto-Watchlist-Pfad auch einen ETF-Watchlist-Eintrag mit Tags, News-Tracking und Alert-Priorisierung ab; der komplette isolierte Lauf gegen den frischen lokalen Build war erfolgreich
- Compose nutzt fuer persistente Backend-Runtime-Daten jetzt standardmaessig `state/runtime/backend-data` statt eines Pfads im Quellbaum
- `ops/automation/deploy.sh` deployt und upgraded jetzt direkt aus Docker Hub, erstellt vor Upgrades Sicherungen und schreibt den aktiven Rollout-Stand nach `state/runtime/deployments/current.env`
- `ops/automation/sync-components.sh` schreibt fuer getaggte Releases jetzt lokale Release-Metadaten unter `state/releases/<IMAGE_TAG>.env`
- `ops/automation/env.sh` laedt fuer lokale Betriebswege jetzt `.env` plus die gitignorierte Override-Datei `.env.local`; `start.sh`, `stop.sh` und `logs.sh` kapseln den Compose-Zugriff mit korrekt gesetztem Projektkontext
- versionierter Docker-Hub-Release-Dry-Run mit `IMAGE_TAG=2026.03.18-testdeploy` wurde erfolgreich ausgefuehrt
- Git-Remote `origin` ist lokal per SSH erreichbar; `publish.yml` synchronisiert Pushes nach `main` jetzt fuer Backend und Frontend automatisch nach Docker Hub als `latest` plus `sha-<commit>` und publiziert Git-Tags `v*` als versionierte Release-Tags
- isolierter Docker-Hub-Deploy-Smoke-Test gegen temporaere Runtime-Pfade und eigene Ports lief erfolgreich durch; Deployment-Record wurde unter `state/runtime/deployments/deployment-20260318T211121Z.env` geschrieben
- `tests/run-upgrade-rehearsal.sh` prueft jetzt initialen Docker-Hub-Deploy, Datenanlage, Upgrade ueber bestehenden Datenbestand, Pre-Upgrade-PostgreSQL-Dump, App-Snapshot und Dump-Restore in einen frischen Stack
- Docker-Hub-Release `2026.03.18-2` wurde erfolgreich gepusht:
  - `dbergt/trading-bot-backend:2026.03.18-2` -> `sha256:29cb70ebac978e0a90e9f95b605638e7894391d21ad0123d2cd6d46536986f82`
  - `dbergt/trading-bot-frontend:2026.03.18-2` -> `sha256:201169e45e11e6edc2fade078cda4d93dfff509dd65d6a42394b7def31fe7167`
- vollstaendiges Docker-Hub-Upgrade-/Restore-Rehearsal fuer `2026.03.18-2` lief erfolgreich durch; der Upgrade-Record liegt unter `state/runtime/deployments/deployment-20260318T213234Z.env`
- Docker-Hub-Release `2026.05.05-1` wurde erfolgreich ueber GitHub Actions `publish #16` gepusht:
  - `dbergt/trading-bot-backend:2026.05.05-1` -> `sha256:9ba0eecf4a1ace9259705191b500fc2b4d0183145076cc34f1702dfabcc4e272`
  - `dbergt/trading-bot-frontend:2026.05.05-1` -> `sha256:973882f6813f9efe7c7f32bbbdccfa4ba7c30c8d4552a4526daf0cb0636159fb`
- vollstaendiges Docker-Hub-Upgrade-/Restore-Rehearsal fuer `2026.05.05-1` lief erfolgreich durch; der Upgrade-Record liegt unter `state/runtime/deployments/deployment-20260505T202750Z.env`
- Docker-Hub-Release `2026.05.07-1` wurde erfolgreich ueber GitHub Actions `publish #21` gepusht:
  - `dbergt/trading-bot-backend:2026.05.07-1` -> `sha256:835f50167496e5bc0fd6e83bdea86bed386590fe88cf71506d759db1380aa4bf`
  - `dbergt/trading-bot-frontend:2026.05.07-1` -> `sha256:f66fe7516f6764bf8a69bd1b920250895013521c73ab6b978165d6846c813d12`
- vollstaendiges Docker-Hub-Upgrade-/Restore-Rehearsal fuer `2026.05.07-1` lief erfolgreich durch; der Upgrade-Record liegt unter `state/runtime/deployments/deployment-20260507T120020Z.env`
- GitHub-Repository `https://github.com/dannybergt/trading-bot-v2.git` ist jetzt initial befuellt; lokales `main` trackt `origin/main`
- der Restore-Blocker im Legacy-Watchlist-Migrationspfad wurde behoben: bei nicht-SQLite-Datenbanken wird der SQLite-Migrationspfad nun sauber uebersprungen statt den Startup zu crashen
- Backend-Logging fuehrt jetzt pro HTTP-Request eine `X-Request-ID`, schreibt strukturierte Request-Metadaten in JSON-Logs und redigiert identifizierende Audit-Werte wie E-Mail-Adressen und Push-Endpoints auf Fingerprints
- Suche, Scanner und Analyse liefern jetzt normalisierte Assetmetadaten (`assetClass`, `assetLabel`, `type`, `market`, `exchange`, `isCrypto`)
- Assetklassifizierung deckt aktuell `stock`, `etf` und `crypto` ab und nutzt Provider-Metadaten mit heuristischen Fallbacks fuer symbolbasierte Anfragen
- Scanner haelt Watchlist-Eintraege auch ohne Live-Bars sichtbar und gibt dabei trotzdem Assetmetadaten zurueck
- API-Regression prueft jetzt Analyse-, Such- und Scanner-Metadaten fuer Stock- und Krypto-Pfade mit dem frisch gebauten lokalen Image erfolgreich
- Watchlist-Items koennen jetzt normalisierte Tags speichern; Add/Update/Delete unterstuetzen auch Krypto-Symbole mit `/` im Symbolpfad
- neuer Endpunkt `/api/watchlists/{id}/news` bindet News pro Watchlist aggregiert an beobachtete Werte, Tags und Assetmetadaten
- neuer Endpunkt `/api/watchlists/{id}/alerts` priorisiert Watchlist-Eintraege aus Signal-, News- und Tag-Kontext als Backend-Basis fuer spaetere Alerts/Popups
- Backup-Export/-Import deckt Watchlist-Item-Tags jetzt mit ab
- API-Regression prueft Watchlist-Tags, Krypto-Item-Management und Watchlist-News-Bindung erfolgreich gegen den isolierten Docker-Stack
- containerisierte Unit-Tests decken jetzt Assetmetadaten, Logging und Alert-Priorisierung ab; API-Regression prueft den neuen Watchlist-Alert-Feed mit
- containerisierte Unit-Tests decken jetzt auch den Bootstrap-Admin-Pfad ab; API-Regression meldet sich mit dem initial gesaeten Admin an
- `tests/run-password-reset-email-smoke.sh` verifiziert die Passwort-Reset-Zustellung lokal ueber einen SMTP-Capture-Server im Docker-Testnetz
- CI und Publish fuehren das API-Regressionsskript jetzt als Gate vor weiterem Build/Push aus
- Compose-Gesamtlauf mit Frontend wurde erfolgreich geprueft
- Frontend-Nginx liefert SPA-Routen aus und proxyt `/api` sowie `/ws` korrekt an das Backend
- `tests/run-ui-regression.sh` startet einen isolierten Stack, fuehrt die Browser-Regression aus und sammelt Artefakte/Logs
- CI und Publish fuehren das UI-Regressionsskript jetzt als Gate vor weiterem Build/Push aus
- `ops/automation/sync-components.sh` unterstuetzt jetzt lokalen Dry-Run und Wiederverwendung vorhandener Images fuer einen technischen Publish-Vorabtest ohne Registry-Push
- `publish.yml` unterstuetzt nun auch einen manuellen `workflow_dispatch`-Dry-Run ohne Docker-Hub-Login/Pushausfuehrung und vermeidet doppelte Builds im Sync-Schritt
- letzter beobachteter GitHub-Actions-`publish`-Run `#3` auf `main` (`13f5c1d`, 2026-03-23 22:11:49 UTC) scheiterte sofort im Step `Run build hook`; Ursache war kein fachlicher Testfehler, sondern fehlende Exec-Bits auf den direkt im Workflow aufgerufenen Shellskripten
- Shellskripte unter `ops/automation/*.sh` und `tests/*.sh`, die im Workflow direkt ausgefuehrt werden, sind jetzt im Git-Stand als executable markiert, damit der naechste echte `publish`-Run nicht erneut am Build-Hook stoppt
- neuer Backend-Pfad `src/backend/app/alpha_vantage_service.py` liefert optional Alpha-Vantage-basierte ETF-Profile/Holdings, ETF-/Krypto-Tageshistorien und providergebundene ETF-/Krypto-News; `MarketDataService` nutzt diese Daten jetzt fuer Watchlists, Alerts, Analyse-Fallbacks und Scanner-Snapshots
- Watchlist-`trackedAssets`, Alert-Items und `/api/stock/{symbol}` tragen fuer ETFs/Krypto jetzt normalisierte Providerdaten (`provider.status/source/quote/research`), sodass die UI den Unterschied zwischen aktivem Livepfad und fehlender Provider-Konfiguration sichtbar machen kann
- das rekonstruierte Frontend zeigt im Dashboard jetzt auch Alpha-Vantage-gebundene ETF-/Krypto-Metadaten direkt auf den `Tracked Assets`- und Alert-Karten; `tests/run-ui-regression.mjs` seedet dafuer jetzt explizit `VOO` und `BTC/USD` und validiert die Provider-Bindung sichtbar
- Watchlist-Alert-Items tragen jetzt zusaetzlich einen normalisierten `providerContext`; das Ranking beruecksichtigt Provider-Live-Status, staerkere Provider-Moves, Research-Verfuegbarkeit und History-Abdeckung
- Alert-Summaries melden jetzt Provider-Coverage-Kennzahlen (`providerLive`, `providerPartial`, `providerUnavailable`, `providerResearch`, `providerMovers`)
- das Dashboard zeigt im Watchlist-Bereich jetzt eine `Provider Coverage`-Sektion mit Live-/Partial-/Research-/Mover-Zahlen und Alpha-Vantage-Highlights fuer ETF-/Krypto-Werte
- neuer Endpunkt `/api/research/{symbol}` liefert Symbol-Research fuer Analyse-Seiten mit Assetprofil, Providerstatus, Quote, ETF-Research, Fundamentals und News; fuer beobachtete Symbole nutzt er Watchlist-Namen als ETF-/Krypto-Klassifizierungsfallback
- das rekonstruierte Frontend rendert auf `/analysis/<symbol>` ein `Provider Research`-Panel mit Providerstatus, Kurs/Move, History-Abdeckung, ETF-Research, Top-Holdings, Headlines und Stock-Fundamentals
- API-Regression prueft den neuen Crypto- und ETF-Research-Kontext; UI-Regression bestaetigt das neue Analyse-Panel mit `ui_symbol_research ok`
- `watchlist_alert_settings` speichert pro Nutzer/Watchlist Alert-Aktivierung, Popup-Schalter, Push-Bereitschaft, Mindestprioritaet und Mindestscore; `/api/watchlists/{id}/alerts` liefert jetzt `alertSettings`, `notificationPlan` und `notification.popupEligible`/`pushEligible`
- Backup/Export/Import decken Watchlist-Alert-Settings mit ab; API-Regression prueft Settings, Notification-Plan und Export/Backup-Abdeckung
- das Dashboard rendert im Watchlist-Bereich ein `Alert Management`-Panel und koppelt In-App-Popups an gespeicherte popup-eligible High-Priority-Alerts; UI-Regression bestaetigt `ui_watchlist_alert_management ok`
- serverseitiger Watchlist-Alert-Dispatcher wertet periodisch Watchlists mit aktivem Push-Schalter aus, nutzt dieselbe Alert-Payload wie `/api/watchlists/{id}/alerts`, sendet Web-Push fuer `pushEligible` Alerts und dedupliziert erfolgreiche Zustellungen ueber `watchlist_alert_deliveries`
- Backup/Export/Import decken die Watchlist-Alert-Delivery-Historie mit ab; Unit-Tests pruefen stabile Dedupe-Keys und die API-Regression prueft die neue Export-/Backup-Flaeche
- Push-/VAPID-Defaults wurden aus Backend und Frontend entfernt; Browser laden den oeffentlichen Key ueber `/api/auth/push/config`, Produktions-/Required-Modus erzwingt vollstaendige VAPID-Konfiguration und `tests/run-push-config-smoke.sh` validiert die Konfiguration ohne echte Push-Zustellung
- `bash ops/automation/build.sh`, `bash ops/automation/test.sh`, `GENERATE_TEST_VAPID=1 bash tests/run-push-config-smoke.sh`, `SKIP_BUILD=1 bash tests/run-api-regression.sh` und `SKIP_BUILD=1 bash tests/run-ui-regression.sh` liefen am 2026-05-07 mit der Push-/VAPID-Haertung erfolgreich durch
- GitHub Actions fuer Commit `21b970a` liefen erfolgreich durch: `ci #24`, `publish #19` und `codeql #30`; der Publish-Run synchronisierte den neuen `sha-21b970aa8b9e`-Stand und `latest`
- Lokale Ziel-VAPID-Werte liegen in `.env.local` (gitignoriert, Modus `600`) und wurden mit `bash tests/run-push-config-smoke.sh` validiert; Werte wurden nicht geloggt
- `bash ops/automation/test.sh`, `SKIP_BUILD=1 bash tests/run-api-regression.sh` und `bash tests/run-ui-regression.sh` liefen am 2026-04-15 mit dem Provider-Coverage-Schnitt erfolgreich durch
- containerisierte Unit-Tests plus isolierte API- und UI-Regression gegen den frischen lokalen Build liefen am 2026-03-26 mit dem neuen ETF-/Krypto-Providerpfad erfolgreich durch
- der nachfolgende GitHub-Actions-`publish`-Run `#4` auf `main` (`4233762`, Start 2026-03-26 21:11:17 UTC, Ende 2026-03-26 21:13:42 UTC) lief bereits durch Build, Test, API-Regression und UI-Regression sauber durch und scheiterte erst im Step `Log in to Docker Hub`
- der aktuellste beobachtete GitHub-Actions-`publish`-Run `#5` auf `main` (`87e4196`, Start 2026-03-27 09:05:13 UTC, Ende 2026-03-27 09:07:36 UTC) bestaetigte denselben Stand erneut: alle Gates gruen, danach Fehler `Username and password required` im Docker-Hub-Login
- `.github/workflows/publish.yml` prueft fehlende Docker-Hub-Secrets jetzt explizit vor `docker/login-action`, damit kuenftige Fehlstarts im Runner eindeutiger benannt werden
- der nachfolgende GitHub-Actions-`publish`-Run `#6` auf `main` (`d4939da`, Start 2026-03-27 11:16:30 UTC, Ende 2026-03-27 11:19:43 UTC) lief vollstaendig erfolgreich durch: Build, Test, API-Regression, UI-Regression, Docker-Hub-Secret-Pruefung, Docker-Hub-Login, `sha-d4939da591ec`-Sync und `latest`-Sync
- Docker-Hub-Image-Pulls fuer den Actions-Publish-Stand wurden am 2026-04-12 lokal bestaetigt:
  - `dbergt/trading-bot-backend:sha-d4939da591ec` -> `sha256:4650bcd75afbd953471bd10144a085cedeb30bc90324677a6ba3d98cb6d6d377`
  - `dbergt/trading-bot-frontend:sha-d4939da591ec` -> `sha256:4c6d0ccfa13717d1f2effeabad32106f3eedc298396c3602e550b38f37cc289e`
- vollstaendiges Docker-Hub-Upgrade-/Restore-Rehearsal fuer `sha-d4939da591ec` lief am 2026-04-12 erfolgreich durch; geprueft wurden initialer Deploy, Datenanlage, Upgrade ueber bestehenden Datenbestand, Pre-Upgrade-PostgreSQL-Dump, App-Snapshot und Dump-Restore in einen frischen Stack
- der zugehoerige Upgrade-Record liegt unter `state/runtime/deployments/deployment-20260412T155353Z.env`
- die SMTP-/Passwort-Reset-Variablen aus `.env.example` werden nun auch im Compose-Backend-Service durchgereicht, damit `PASSWORD_RESET_BASE_URL`, `SMTP_*` und `ENABLE_INSECURE_DEBUG_RESET_TOKENS` im lokalen Compose-Lauf wirksam werden
- `tests/run-alpha-vantage-live-smoke.sh` prueft bei gesetztem `ALPHA_VANTAGE_API_KEY` echte Alpha-Vantage-Snapshots fuer `VOO` und `BTC/USD` sowie deren Durchreichung ueber `MarketDataService`
- die Alpha-Vantage-BTC-Antwortstruktur wurde am 2026-04-14 ohne API-Key-Ausgabe geprueft; `DIGITAL_CURRENCY_DAILY` liefert aktuell generische OHLC-Keys (`1. open`, `2. high`, `3. low`, `4. close`), und der Backend-Parser akzeptiert jetzt diese Form sowie die alten waehrungsspezifischen `1a./1b.`-Keys
- der Alpha-Vantage-Live-Smoke lief am 2026-04-14 gegen `trading-bot-v2-backend:local` erfolgreich durch: `VOO` History/ETF-Profil live, `BTC/USD` 30 History-Zeilen live, `MarketDataService` liefert fuer beide Symbole `status=live`
- GitHub Actions fuer Commit `f826304` liefen erfolgreich durch (`ci`, `publish`, `codeql`); der Docker-Hub-Backend-Tag `sha-f826304a7850` wurde lokal gepullt und live-smoke-validiert
- das Upgrade-/Restore-Rehearsal fuer `sha-f826304a7850` lief nach Haertung der Rehearsal-Env erfolgreich durch; Upgrade-Record `state/runtime/deployments/deployment-20260414T192521Z.env`
- `tests/run-upgrade-rehearsal.sh` deaktiviert fuer seine isolierten Stacks explizit Bootstrap-Admin-Werte aus `.env`, und `ops/automation/deploy.sh` respektiert diese Shell-Overrides, damit echte Zielumgebungs-Admins lokale Wegwerf-Rehearsals nicht beeinflussen
- GitHub Actions fuer Commit `9c2f2b` liefen erfolgreich durch: `publish` run `24423017757`, `ci` run `24423017764`, `codeql` run `24423017762`; der Publish-Run synchronisierte `sha-9c2f2b08fa76` und `latest`
- GitHub Actions fuer Commit `df6f0fa` liefen erfolgreich durch: `publish` run `24461808225`, `ci` run `24461808223`, `codeql` run `24461808224`; der Publish-Run synchronisierte `sha-df6f0fa13a5d` und `latest`
- Passwort-Reset kann jetzt ueber SMTP an einen konfigurierbaren Frontend-Reset-Link zugestellt werden
- SMTP-Reset-Zustellung wurde lokal erfolgreich gegen einen Testserver inklusive Link-Extraktion, Confirm und Re-Login verifiziert
- Docker-Hub-Publish wurde lokal erfolgreich ausgefuehrt:
  - `dbergt/trading-bot-backend:latest` -> `sha256:6ccd0f476169e184d30c80b7dacdb8f99b6fee65e92f98c8c7a722205052bf84`
  - `dbergt/trading-bot-frontend:latest` -> `sha256:201169e45e11e6edc2fade078cda4d93dfff509dd65d6a42394b7def31fe7167`
- Compose-Testdaten wurden nach den Verifikationen wieder bereinigt

## Offene Punkte

- Originaler Frontend-Quellstand fehlt
- Persistenz- und Migrationsstrategie ist fuer weiteres Wachstum zu schwach
- keine belastbare Test-Suite fuer Kernfluesse vorhanden
- kuenftige Release-Tags muessen denselben Upgrade-/Restore-Rehearsal-Pfad erneut bestehen
- der automatische GitHub-Actions-Publish-Pfad ist fuer `main` live bestaetigt; bei kuenftigen Workflow-Aenderungen weiter auf Secret-/Namespace-Drift achten
- alte Docker-Hub-Staende vor `sha-f826304a7850` enthalten den BTC-Parserfix noch nicht; fuer Live-Smokes mit BTC/USD mindestens `sha-f826304a7850` oder neuer verwenden
- das Backend-Docker-Hub-Repo ist ueber die unauthentifizierte Docker-Hub-API nicht sichtbar; zur Verifikation daher `docker pull` mit lokaler Authentifizierung oder GitHub-Actions-Logs nutzen

## Naechste Schritte

- Wellen 1-12 + Welle 14 + Phase 4 Auto-Execution-Infrastruktur ausgeliefert. **Naechster Schnitt: Phase 4-Folge** entweder (A) echter Auto-Trade-Loop der periodisch `evaluate_proposal` aufruft und allowed=true Vorschlaege in echte Alpaca-Orders uebersetzt, mit Reconciliation-Task der Alpaca-Order-Status periodisch gegen lokale DB abgleicht und bei Discrepancy korrigiert (= Phase 4e), ODER (B) zuerst Welle 11 Phase B / Welle 13 / weitere Datenbasis-Tiefe wenn der User den Auto-Loop noch nicht anschalten will. Voreilig sollte der Auto-Loop NICHT angeschaltet werden — Defaults bleiben OFF und ein expliziter User-Go ist Pflicht.
- Welle 13 (optional): FinBERT-Image-Variant `dbergt/trading-bot-backend-finbert` als zweite Build-Stage. Welle 11 Phase B (Capacitor + Biometric + App-Store) zurueckgestellt bis echter Native-App-Bedarf.
- Multi-Agent-Port-Disziplin: vor Compose-/Regression-Laeufen `docker ps | grep -E '180[89]|181[0-9]{2}'` pruefen, bei Treffer Env-Vars (`BACKEND_PORT`, `FRONTEND_PORT`, `PRIMARY_BACKEND_PORT`, `RESTORE_BACKEND_PORT`) verschieben, fremde Stacks nie killen.
- denselben Rehearsal-Pfad fuer jeden neuen Release-Tag diszipliniert wiederholen
- GitHub-Actions-`publish` bei kuenftigen `main`-Pushes weiter beobachten, aber nicht mehr als aktueller Blocker behandeln
- `current.env` nur fuer echte Zielumgebungen und nicht fuer Smoke-Staende schreiben
- Frontend-Quellstand beschaffen oder kontrolliert rekonstruieren
- die rekonstruierte Bundle-Patch-Schicht bei kuenftigen Frontend-Image-Updates mitpruefen, damit der nachgeruestete Settings-/Admin-Navigationsfix nicht verloren geht
- den neuen Watchlist-Alerts-/News-/Tracked-Assets-Patch bei kuenftigen Frontend-Image-Updates mitpruefen oder in echten Frontend-Quellstand ueberfuehren
- echte Zielumgebungs-VAPID-Werte setzen und mit `bash tests/run-push-config-smoke.sh` pruefen, sobald produktive Push-Zustellung aktiviert werden soll
- Assetklassifizierung, Watchlist-Tags, News-Bindung und Alert-Priorisierung aus dem aktuellen Dashboard-Patch in weitere UI-Flows und spaeter in echten Frontend-Quellstand ueberfuehren
- echte Live-Daten-/Providerpfade fuer ETFs und Krypto weiter schliessen, damit Phase 1 nicht auf Fallback-Metadaten, gecachten Leerantworten und spaerlichen Newsfeeds stehen bleibt
- Watchlist-Alert-Dispatcher nach Produktions-VAPID-Haertung optional um nutzergebundene In-App-/WebSocket-Zustellung erweitern
- Datenmodell und Migrationen professionalisieren
- verbleibende Logging-/Telemetry-Haertung fuer Background-Jobs und WebSockets nachziehen
- Phase 1 Produktausbau gem. `docs/admin/product-roadmap.md` konsequent weiterziehen
